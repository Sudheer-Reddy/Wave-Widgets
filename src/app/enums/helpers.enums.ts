enum PictureShape {
    circle,
    rounded,
    thumbnail
}

enum Animation {

}

enum PictureAspect {
    None,
    Both,
    H,
    V
}

enum BackgroundAttachment {

}

enum Cursor {
    default,
    pointer
}

export {PictureAspect, Animation, PictureShape, BackgroundAttachment, Cursor}